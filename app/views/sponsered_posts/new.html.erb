<h1>New Sponsered Post</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for Sponsered Posts</p>
    <ul>
      <li>Make sure it rhymes</li>
      <li>Don't use the letter "A".</li>
      <li>The incessant use of hashtags will get you banned</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for [@topic, @sponsered_post] do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter sponsered post title" %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter sponsered post body" %>
      </div>
      <div class ="form-group">
        <%= f.label :price %>
        <%= f.number_field :price, class: 'form-control', placeholder: "Enter price" %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
</div>
